// naming convention question
export interface Product {
  id: number;
  title: string;
  price: number;
  description: string;
  category: string;
  image: string;
}

// incorporate rating for rate and count to be type num